name: Caller

on: workflow_dispatch     # manual only — click "Run workflow" in the Actions tab

jobs:
  call-the-tests:
    uses: ./.github/workflows/reusable-tests.yml   # call it by path
    with:
      node-version: '20'