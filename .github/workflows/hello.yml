name: Hello World
on: push
jobs:
    greet:
        runs-on: ubuntu-latest
        steps:
            - name: Say hello
              run: echo "Hello, Github Actions!"
            - name: Show the date
              run: date